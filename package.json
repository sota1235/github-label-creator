{
  "private": true,
  "name": "github-issue-creater",
  "scripts": {
    "build": "babel src --out-dir dest --source-maps-inline",
    "watch": "babel src --out-dir dest --watch --source-maps-inline",
    "lint": "eslint src/"
  },
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "babel-preset-es2015": "^6.16.0",
    "babel-preset-es2016": "^6.16.0",
    "eslint": "^3.8.1"
  }
}
